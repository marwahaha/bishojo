<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Bishojo" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>

    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <%= nav_bar :fixed => :top, :brand => 'Bishojo', :responsive => true, :inverse => true do %>
      <%= menu_group do %>
        <%= menu_item "Home", root_path %>
        <%= menu_item "Games", games_path %>
      <% end %>
      
      <%= menu_group :pull => :right do %>
        <%= menu_text "We make shiny things" %>
        <% if user_signed_in? %>
          <%= drop_down current_user.name do %>
            <%= menu_item "Games", user_games_path(current_user) %>
            <%= drop_down_divider %>
            <%= menu_item "Edit Account", account_path %>
            <%= menu_item "Edit Account Settings", account_settings_path %>
        <% end %>
          <%= menu_item "Logout", destroy_user_session_path %>
        <% else %>
          <%= menu_item "Login", new_user_session_path %>
          <%= menu_item "Register", new_user_registration_path %>
        <% end %>
      <% end %>
    <% end %>

    <div class="container">
      <div class="row">
        <div class="col-lg-12">
          <%= bootstrap_flash %>
          <%= render_breadcrumbs(" / ", { :class => '', :item_class => '', :divider_class => 'hidden', :active_class => 'active' }) %>
          <%= yield %>
        </div>
      </div><!--/row-->

      <!--<footer>-->
      <!--  <p>&copy; Company 2015</p>-->
      <!--</footer>-->

    </div> <!-- /container -->
    
    <%= nav_bar :fixed => :bottom, :responsive => true, :inverse => true do %>
      <%= menu_group :pull => :right do %>
        <%= menu_item "Contact", contact_path %>
        <%= menu_item "Terms", terms_path %>
        <%= menu_item "Impressum", impressum_path %>
        <%= menu_item "Privacy", privacy_path %>
      <% end %>
    <% end %>

  </body>
</html>
